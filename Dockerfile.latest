# Dockerfile для latest варианта
FROM python:3.11-slim

# Рабочая директория
WORKDIR /app

# Скопировать приложение
COPY latest/ /app/

# Обновить pip и установить инструменты аудита globally
RUN pip install --upgrade pip \
    && pip install pip-audit safety

# Установить зависимости приложения
RUN pip install -r requirements.txt

# Простейший entrypoint — печать строки
CMD ["python", "app.py"]
